<!doctype html>
<html lang="ja">
<head>
  <meta charset="utf-8" />
  <title>Tsundere Bot History</title>
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif; margin: 24px; }
    main { max-width: 800px; margin: 0 auto; }
    .toolbar { display: flex; gap: 8px; margin-bottom: 12px; }
    .btn { padding: 6px 10px; border: 1px solid #ddd; background:#fafafa; border-radius:6px; cursor:pointer; }
    .btn:hover { background:#f0f0f0; }
    .btn.primary { background:#2563eb; color:#fff; border-color:#1e40af; }
    .btn.primary:hover { background:#1d4ed8; }
    .btn.danger { background:#fee2e2; border-color:#fecaca; }
    .meta { font-size: 12px; color:#666; margin-bottom:4px; }
    .body { white-space: pre-wrap; line-height:1.5; }
    .item { border-bottom:1px solid #eee; padding:10px 0; }
    .actions { margin-top:6px; }
    .editor { background:#f8fafc; border:1px solid #e2e8f0; padding:10px; border-radius:8px; margin-top:8px; }
    .editor-row { margin-bottom:8px; }
    .label { margin-right:8px; font-size:12px; color:#444; }
    .w-full { width:100%; }
    .select { padding:4px; }
    .toast { position:fixed; right:12px; bottom:12px; background:#333; color:#fff; padding:8px 12px; border-radius:8px; opacity:.95; z-index:9999; }
  </style>
</head>
<body>
  <main>
    <div class="toolbar">
      <button id="reloadHistory" class="btn">履歴を再読込</button>
      <button id="toggleSort" class="btn">並び: 新→古</button>
    </div>
    <ul id="historyList"></ul>
  </main>
  <script type="module" src="/frontend/main.js"></script>
</body>
</html>
